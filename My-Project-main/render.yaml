# render.yaml – Render Blueprint for Akshay D Portfolio
# Docs: https://render.com/docs/blueprint-spec

services:
  - type: web
    name: akshay-d-portfolio
    plan: free
    runtime: python
    rootDir: My-Project-main
    buildCommand: pip install -r requirements.txt
    startCommand: python -m gunicorn app:app
    envVars:
      - key: FLASK_ENV
        value: production
      - key: SECRET_KEY
        generateValue: true
      - key: PYTHON_VERSION
        value: 3.11.15
